# Only needed for `make full` (live LLM calls).
# `make demo` replays reports/../cache/llm/*.json and needs NO key.
OPENAI_API_KEY=sk-...
# Optional: overrides for the two model roles (see src/support_agent/config.py)
# GENERATOR_MODEL=gpt-4o-mini
# JUDGE_MODEL=gpt-4.1-mini
